# Legacy Host-Tools Notice

The project-owned bootstrap shim, setup scripts, and udev rules in this legacy
platform copy are distributed under the accompanying MIT License.

This compatibility copy does not redistribute pyOCD or dependency wheels. The
setup helper installs the version pinned in `requirements-pyocd.txt` from the
user's configured Python package index. Those separately obtained packages
retain their own licenses, notices, and source-delivery mechanisms.
